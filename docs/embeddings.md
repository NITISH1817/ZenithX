# Embeddings Module

## Purpose

Convert cleaned OCR text into semantic vectors that capture meaning.

## Example

Text:

transaction successful amazon pay

↓

Embedding Vector

[0.12, -0.45, ...]

## Benefits

Allows semantic search instead of exact keyword matching.

Example:

Search:
UPI payment

Can retrieve:
Transaction successful Amazon Pay

Even when words are different.